/** Numbers the legacy ownership migration runs on. */

/** The backup directory is private to the server's user. */
export const BACKUP_DIR_MODE = 0o700;
/** The ownership mapping file is private to the server's user. */
export const MAPPING_FILE_MODE = 0o600;
/** Indentation of the ownership mapping file, for people reading it. */
export const MAPPING_INDENT = 2;
