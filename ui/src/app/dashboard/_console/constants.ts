/**
 * Every number the console page runs on, by name: how often each feed polls.
 */

/** How often the browser table refreshes. */
export const BROWSERS_POLL_MS = 3000;
/** How often the fleet strip refreshes. */
export const FLEET_POLL_MS = 5000;
/** How often the personas refresh. */
export const PERSONAS_POLL_MS = 10_000;
/** How often relative times ("12s ago") advance. */
export const CLOCK_TICK_MS = 5000;
/** Milliseconds in a minute, for the per-minute command rate. */
export const MS_PER_MINUTE = 60_000;
/** Fraction to percent. */
export const PERCENT = 100;
